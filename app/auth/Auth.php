<?php

namespace App\auth;
/**
* 
*/
class Auth
{
	private $auth = false;
	/*public function __construct($uid, $hash, $configVk)
	{
		/*
		Проверяем авторизацию ииии
		*/
		/*if(isset($_SESSION['uid']) and isset($_SESSION['hash'] ))
		$container = $app->getContainer();
		$vkSettings = $container->get('settings')['vk'];
		$hash = $vkSettings['app_id'] . $requestVkAuth['uid'] . $vkSettings['client_secret'];
		if (md5($hash) === $requestVkAuth['hash']) {
			echo "Авторизовался!";
		}
	}*/
}